## Tidewave

Always use Tidewave's tools when available:
- Use `get_docs` to check documentation instead of guessing APIs
- Use `get_source_location` to find module/function definitions
- Use `project_eval` to test expressions in the running app
- Use `get_logs` to inspect runtime behavior